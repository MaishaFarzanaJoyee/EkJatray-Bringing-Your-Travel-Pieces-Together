export default function AdminPage() {
  return (
    <main className="page-center">
      <section className="card auth-card">
        <h2>Admin Page</h2>
        <p className="description">Admin panel migration will be added in a later step.</p>
      </section>
    </main>
  );
}
